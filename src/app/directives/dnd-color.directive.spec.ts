import { DndColorDirective } from './dnd-color.directive';

describe('DndColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DndColorDirective();
    expect(directive).toBeTruthy();
  });
});
